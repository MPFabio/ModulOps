_format_version: "3.0"
_transform: true

# Chaque service backend est ciblé via un objet `upstream` (plutôt qu'une
# URL `http://<nom>:<port>` directe) avec des health checks actifs activés.
# Sans ça, le balancer DNS de Kong met en cache l'IP résolue d'un conteneur
# pour la durée de vie du worker nginx : quand `docker compose up --build`
# recrée un service (nouvelle IP Docker), Kong continue de cibler l'ancienne
# IP et renvoie 502 "Connection refused" jusqu'à un redémarrage manuel.
# Les health checks actifs (intervalle court) détectent la cible morte,
# la marquent unhealthy, et le balancer ré-interroge le DNS Docker
# (127.0.0.11) pour retrouver la nouvelle IP du conteneur recréé.
upstreams:
- name: auth-service-upstream
  targets:
  - target: auth-service:8080
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: k8s-service-upstream
  targets:
  - target: k8s-service:8081
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: terraform-service-upstream
  targets:
  - target: terraform-service:8082
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: ansible-service-upstream
  targets:
  - target: ansible-service:8083
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: metrics-service-upstream
  targets:
  - target: metrics-service:8086
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: code-service-upstream
  targets:
  - target: code-service:8088
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: vault-service-upstream
  targets:
  - target: vault-service:8087
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

- name: pipeline-service-upstream
  targets:
  - target: pipeline-service:8084
  healthchecks:
    active:
      type: http
      http_path: /health
      timeout: 5
      concurrency: 1
      healthy:
        interval: 5
        successes: 1
      unhealthy:
        interval: 2
        # Trois échecs consécutifs avant de retirer une instance du pool.
        # Avec un seuil de 1, une seule réponse lente — un service Go sous
        # charge, un pic d'entrées/sorties — suffisait à sortir le service et à
        # renvoyer 503 aux utilisateurs, alors qu'il redevenait sain dans les
        # trois secondes. Le délai passe aussi de 2 à 5 s : une sonde de survie
        # doit constater une panne, pas mesurer la latence.
        http_failures: 3
        tcp_failures: 3
        timeouts: 3

services:
- name: auth-service
  url: http://auth-service-upstream
  routes:
  - name: auth-route
    paths:
    - /api/v1/auth
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - DELETE
    plugins:
    - name: rate-limiting
      config:
        minute: 20
        hour: 200
        policy: local
        limit_by: ip
        error_message: "Trop de tentatives d'authentification. Veuillez réessayer plus tard."
  - name: projects-route
    paths:
    - /api/v1/projects
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - OPTIONS

- name: k8s-service
  url: http://k8s-service-upstream
  connect_timeout: 15000   # 15s pour établir la connexion
  read_timeout: 120000     # 120s (auth GKE + appel API peuvent être lents)
  write_timeout: 60000     # 60s
  routes:
  - name: k8s-route
    paths:
    - /api/v1/k8s
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    protocols:
    - http
    - https

- name: terraform-service
  url: http://terraform-service-upstream
  # La detection de drift lance `tofu init` puis `plan -refresh-only` : au
  # premier appel, le telechargement du provider depasse le defaut de 60 s.
  connect_timeout: 60000
  write_timeout: 300000
  read_timeout: 300000
  routes:
  - name: terraform-route
    paths:
    - /api/v1/terraform
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - DELETE

- name: ansible-service
  url: http://ansible-service-upstream
  routes:
  - name: ansible-route
    paths:
    - /api/v1/ansible
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - DELETE
  - name: ansible-webhooks-route
    paths:
    - /api/v1/webhooks/ansible
    strip_path: false
    methods:
    - POST

- name: metrics-service
  url: http://metrics-service-upstream
  routes:
  - name: metrics-route
    paths:
    - /api/v1/metrics
    strip_path: false
    methods:
    - GET
    - OPTIONS

- name: code-service
  url: http://code-service-upstream
  routes:
  - name: code-route
    paths:
    - /api/v1/code
    strip_path: false
    methods:
    - GET
    - OPTIONS

- name: vault-service
  url: http://vault-service-upstream
  routes:
  - name: vault-route
    paths:
    - /api/v1/vault
    strip_path: false
    methods:
    - GET
    - POST
    - DELETE
    - OPTIONS

- name: pipeline-service
  url: http://pipeline-service-upstream
  routes:
  - name: pipeline-route
    paths:
    - /api/v1/pipeline
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS

# Route de repli vers l'interface. Kong retient le préfixe le plus long, les
# routes d'API ci-dessus gardent donc la priorité sur celle-ci, qui ne reçoit
# que ce qu'aucune autre ne réclame : les pages et les fichiers du bundle.
#
# Cette entrée n'existe que dans le chart, et c'est voulu : sous Docker Compose
# le frontend publie son propre port et se joint directement, alors que sur le
# cluster l'Ingress dirige tout vers la passerelle. Sans elle, l'interface
# répond 404 alors que toutes les API fonctionnent. À conserver lors d'un
# réalignement des deux configurations.
- name: frontend
  url: http://frontend:8080
  routes:
  - name: frontend-route
    paths:
    - /
    strip_path: false
    methods:
    - GET
    - HEAD
    - OPTIONS

plugins:
# Expose les métriques Kong sur la Status API (/metrics), scrapées par vmagent.
- name: prometheus
  config:
    status_code_metrics: true
    latency_metrics: true
    upstream_health_metrics: true
# Garde-fou global contre une boucle folle ou un client qui s'emballe, et non
# frein a l'usage. Les valeurs precedentes, cent par minute et mille par heure,
# etaient sous l'usage nominal : l'interface emet une dizaine d'appels par page,
# et douze ingenieurs sortant par une meme adresse publique se les partageaient.
#
# Elles contredisaient surtout la mise a l'echelle : un plafond de passerelle
# doit rester au-dessus de la capacite que l'autoscaling est cense fournir,
# sinon il l'annule. Aucun autoscaler ne se declenche derriere une passerelle
# qui laisse passer moins de deux requetes par seconde, et les plafonds de six,
# cinq et quatre replicas n'auraient jamais servi. Constate par la campagne de
# charge, qui n'a mesure que le limiteur tant qu'il est reste a ce niveau.
#
# Le plafond se cale sur la capacite maximale que la mise a l'echelle peut
# fournir, et non sur une valeur ronde. Mesure : deux replicas d'auth-service
# tiennent quatre-vingt-dix requetes par seconde a quarante pour cent de leur
# requete CPU, soit environ soixante-dix-huit par replica au seuil de
# declenchement. Les six replicas du plafond en absorbent donc pres de cinq
# cents. Limiter la passerelle en dessous rendrait ces replicas inatteignables :
# on paierait un autoscaler qui ne peut pas se declencher.
#
# Cinq cents requetes par seconde restent une protection reelle : une boucle
# folle l'atteint en quelques secondes, douze utilisateurs jamais.
#
# La protection contre la force brute reste sur /api/v1/auth, ou elle a un objet
# precis, avec ses vingt requetes par minute.
- name: rate-limiting
  config:
    minute: 30000
    hour: 500000
    policy: local
- name: cors
  config:
    origins:
    - "*"
    methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
    headers:
    - Accept
    - Accept-Version
    - Content-Length
    - Content-MD5
    - Content-Type
    - Date
    - Authorization
    - X-Project-ID
    exposed_headers:
    - X-Auth-Token
    credentials: true
    max_age: 3600
